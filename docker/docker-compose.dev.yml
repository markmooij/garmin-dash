# Development Docker Compose (dev machine, amd64)
# Run: docker compose -f docker-compose.dev.yml up -d
# Reads .env from this directory if present (optional in dev).

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    ports:
      - "${GARMINDASH_PORT:-8000}:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - DOCKER_PROFILE=dev
      - DEBUG=true
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
    networks:
      - garmin-dash

  scheduler:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    restart: unless-stopped
    command: ["sh", "-c", "gdash-migrate && exec gdash ingest schedule"]
    env_file:
      - path: .env
        required: false
    environment:
      - DOCKER_PROFILE=dev
      - DEBUG=true
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      - SIGNAL_CLI_API_URL=http://signal-api:8080
    volumes:
      - ../data:/app/data
      - ../logs:/app/logs
    networks:
      - garmin-dash

  # Signal REST API (Phase 4 — only when provisioned):
  #   docker compose -f docker-compose.dev.yml --profile signal up -d
  signal-api:
    image: bbernhard/signal-cli-rest-api:latest
    profiles: ["signal"]
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # signal-cli stores its data in /home/.local/share/signal-cli
      # (SIGNAL_CLI_CONFIG_DIR default) — this is what must persist.
      - ../data/signal:/home/.local/share/signal-cli
    environment:
      - SIGNAL_CLI_BIN=/usr/local/bin/signal-cli
    networks:
      - garmin-dash

networks:
  garmin-dash:
    driver: bridge
